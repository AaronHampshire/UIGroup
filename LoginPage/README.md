# Login Page (UI Kata)
This is a login screen built using Tailwind CSS. 

## Dependencies
* Install node/npm. 
* Install gulp globally:
    ```
    npm install gulp-cli -g
    ```

## Build
* `npm install`
* `gulp`